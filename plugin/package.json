{
  "name": "control-tower-loop",
  "version": "0.57.0",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && vitest run",
    "test:fast": "vitest run --exclude '**/*-real-process.test.js'"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "vitest": "^4.0.0",
    "yaml": "^2.5.0"
  }
}
