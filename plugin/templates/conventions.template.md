# This repo's yardstick — the documents that hold the code's rules

<!-- ct-step reads this file DIRECTLY and pastes it into every task's brief:
     the implementer writes with this in front of them, and the judge blocks
     citing it. It is a property of the REPO, not of any epic: it is declared
     ONCE here, not in section 3 of every slice plan.
     NOTE: this is not .agent/conventions-ack.md (acknowledgements of the
     loop's protocol collision signals) — this file declares HOW code is
     written here. -->

Rules to obey (one path per line, in backticks; it must be readable):

- (ninguna declarada todavía — replace this line when you declare the first one)

Skills (skill name, not path):

- (none)
