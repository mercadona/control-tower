#!/usr/bin/env node
// A DEDICATED stub to simulate "cmux not available" (finding 2, an adversarial
// attack): cmux IS installed on the real development machine — a test that
// simply leaves `fake-cmux-bin` out of its PATH but keeps `process.env.PATH` at
// the end ENDS UP FINDING THE REAL CMUX (verified by construction: exactly the
// mistake an earlier agent of this very project made, launching a real
// workspace by assuming "not installed"). This stub, placed AHEAD of the real
// PATH in the tests that need to simulate absence/failure, ALWAYS fails — it
// never delegates to the real cmux, and it never really runs `new-workspace`.
process.exitCode = 127
