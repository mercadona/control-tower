{
  "issue": 161,
  "tasks_total": 5,
  "verdict": {
    "ruling": "PASS",
    "rubric": [
      {
        "rule": "estado-final",
        "result": "Las once frases de `### Desired end state`, una a una contra la suma del diff. (1) 'El brief de una tarea que no crea fichero incluye architecture.md, y el paquete de esa misma tarea también': `escribirBrief` pasa a `PluginYardstick.composeSection(deCt)` y `escribirPaquete` a `composePathSection(cargarVaraDeCt())`, las dos sin filtro (ct-step.mjs); la mitad del brief la fija el test 'una tarea que no estrena módulo se lleva la vara entera, architecture.md incluido'. Observación sin hallazgo: la mitad del PAQUETE se queda sin test propio (el que la medía en ct-step-paquete.test.js se retiró y el **Tests:** de la tarea 1 no manda sustituto), pero la línea de producción que la promete SÍ está en el diff, que es lo que este ítem mide, y la lista de retirados la escribió el plan. (2) 'creaModulo ya no existe en ct-step.mjs; forTask, appliesToTask, NEW_MODULES y scopeOf ya no existen en plugin-yardstick.js': grep sobre todo `plugin/` tras el último commit devuelve una única aparición de cualquiera de esos nombres, y es la cadena 'MÓDULOS NUEVOS' dentro del `not.toContain` del test nuevo de kickoff — los cinco símbolos están borrados. (3) 'El kickoff ya no reparte la arquitectura entre (create) y (modify)': la entrada entera de kickoff.js desaparece y kickoff.test.js lo fija. (4) 'Un plan enmendado a media tarea acaba DENTRO del commit de esa tarea, sin commit nuevo': `esDelRun` pierde su cláusula del plan y `entradasDelArbol` deja pasar `rutaDelPlan()` por encima de la guarda de la maquinaria; el test mide `git show --name-only HEAD` con plan.md y `commits() === 2`. (5) 'controls sale 0 con el plan stageado y su salida no nombra la ruta del plan': `rutasDeTrabajoEnElIndice()` filtra el plan en los tres lectores de contenido (`alcanceDeclarado`, `enElIndice`, `bloquesDeclarados`) y el test lo comprueba sobre el log. (6) 'El paquete de revisión de la tarea trae el diff del plan': `diffDeTarea()` es `git diff --cached -U10`, y el plan ya está en el índice; test 'el paquete de revisión de la tarea trae el diff del plan'. (7) 'Ningún verbo posterior sale con PRECONDITION': test con reconcile, global y slice-verdict contra el código 8. (8) 'Un veredicto vetado devuelve el árbol y el plan de HEAD no declara la ruta añadida': `limpiarElArbolDeLaTarea` reutiliza `entradasDelArbol`, que ahora incluye el plan; test del segundo veto y el consejo. (9) 'Una enmienda que QUITA una ruta declarada se rechaza, el paso sale en rojo diciendo por qué, y el plan de HEAD la sigue declarando': `enmiendaSoloAnade(t)` (ct-step.mjs:1400) contra `git show HEAD:<plan>` con `extractTasks`, llamada desde `verboControls` con peso fatal y cabecera '# enmienda del plan'. (10) 'El fichero de estado y .agent/run-<n>/ se quedan fuera del commit': `esDelRun` conserva esas dos exclusiones y el test asserta que el commit no nombra .agent/. (11) 'La suite de plugin/ en verde': la Global verification ya corrió y salió en verde. Ninguna promesa la deshace una tarea posterior, y ninguna se queda sin línea que la produzca.",
        "outcome": "conforme"
      },
      {
        "rule": "coherencia",
        "result": "Cinco commits, así que el ítem tiene sujeto. Los revisé por pares en las cuatro fronteras donde se tocan. Tarea 1 (borra el filtro en código) y tarea 2 (retira la prosa que lo afirmaba) son las dos mitades de D-3 en el orden que D-6 fija, y la tarea 2 no reintroduce nada de lo que la 1 quitó: la cabecera de precedencia que la 1 reescribió sigue diciendo 'los ocho documentos alcanzan a todo diff' y la 2 sólo cambia architecture.md, kickoff.js, task-implementer.md y ct-judge.md; el mapa ALCANCES del test pasa a 'every diff' en el mismo commit que la cabecera del documento, así que no queda ninguna ventana en que el documento y su test discrepen. Tarea 3 (el plan entra en `entradasDelArbol` y sale de los tres controles de contenido) y tarea 4 (`enmiendaSoloAnade`) resuelven dos direcciones distintas del mismo control y se apoyan una en la otra en vez de duplicarse: la 4 lee `stagedPaths()` crudo a propósito, porque su pregunta es de pertenencia (¿está el plan en el índice?), justo la distinción que el comentario de `rutasDeTrabajoEnElIndice` de la tarea 3 deja escrita. La tarea 4 no apaga nada de la tarea 3: su guardián sólo dispara cuando la enmienda RETIRA una ruta, así que el caso feliz de la 3 ('el control de alcance sale 0 y su salida no nombra la ruta del plan') sigue verde, y de hecho la 4 le añade el test hermano 'una enmienda que sólo añade rutas pasa el control'. Tarea 4 y tarea 5 no se contradicen: la frase que la 5 escribe en ct-judge.md ('The program already refused any amendment that removes a declared route') es cierta porque la 4 la implementó antes, y la 5 llega después en el orden de commits. Tarea 2 y tarea 5 tocan el mismo fichero, ct-judge.md, en ítems distintos (el dos del recorte de patrones y el ocho de alcance) y ninguna pisa la otra. No hay andamio intermedio que una tarea posterior tuviera que retirar y no retirase: el fichero de test que estrena la 3 lo amplía la 4, y ningún helper temporal queda vivo (las madres `withScopes` y `theRealOnesOnDisk` se borraron en la misma tarea que dejó sin llamantes). Los cinco veredictos de tarea ya están en PASS y sus dos hallazgos bajos (el bloque de prosa desfasado de `limpiarElArbolDeLaTarea` y el tercer test de la tarea 4) son locales a su tarea y ya están en el registro: no los recuento aquí.",
        "outcome": "conforme"
      },
      {
        "rule": "observabilidad",
        "result": "No hay vara que aplicar: la sección `## Señal` del paquete dice literalmente '(sin señal declarada — el issue no trae la sección \"## Señal de observabilidad\"; el juez de slice mide su ítem observabilidad como sin-vara)'. El slice nunca fue requerido a prometer una señal, así que no invento una para medirlo, ni cargo el olfato de la señal redundante, que sólo alcanza a una celda que sí declara algo.",
        "outcome": "sin-vara"
      }
    ],
    "findings": [],
    "review_token": "8374e91c9ec1760d2e95bf6eb4a03901df78642b8a5cbe84dcbb09b65a81a7fc"
  }
}
