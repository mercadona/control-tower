command: gh api graphql --paginate --slurp -f query=query($endCursor:String,$owner:String!,$name:String!){ repository(owner:$owner,name:$name){ issues(first:100,after:$endCursor,states:[OPEN],orderBy:{field:CREATED_AT,direction:ASC}){ nodes{ number url title body state stateReason milestone{number title description} labels(first:50){nodes{name}} } pageInfo{ hasNextPage endCursor } } } } -f owner=jjponz -f name=repo-pulse
version: gh version 2.100.0 (2026-09-03)
date: 2026-09-25
exit: 0
--- stdout
[{"data":{"repository":{"issues":{"nodes":[{"number":7,"url":"https://github.com/jjponz/repo-pulse/issues/7","title":"#7 UI: estados no felices","body":"> Slice `#7` del epic. Spec: [docs/superpowers/specs/2026-08-13-repo-pulse-mvp-execution.md § Tabla de slices](https://github.com/jjponz/repo-pulse/blob/main/docs/superpowers/specs/2026-08-13-repo-pulse-mvp-execution.md#tabla-de-slices)\n\n## Descripción\nsobre las vistas de #5 y #6: carga indeterminada\\, ventana sin actividad con CTA\\, no-repo\\, sin commits\\, banner de foto desactualizada\n\n## Contexto del epic\n- Stack: Node ≥22, TypeScript estricto; npm workspaces: `web/` (Vite+React) y `server/` (Express); tests con Vitest.\n- `server/src/analysis/` es un módulo puro y el ÚNICO código que ejecuta `git`.\n- Referencia visual obligatoria: `docs/design/repo-pulse-mockup.html`; los chips \"estado de demo\" del pie NO se implementan.\n- Los nombres de autores no salen NUNCA del servidor: ni en payloads de API ni en el DOM — solo conteos y porcentajes.\n- Cálculo: HEAD del clon sin merge commits; autor = email en minúsculas respetando `.mailmap`; concentración = mínimo nº de autores que suma ≥80% de los commits.\n- Ventanas: `30d`, `90d`, `12m` (default), `all`; cubos: día (30d), semana (90d, 12m), mes (all); tendencia = ventana actual vs anterior de igual longitud; en `all` no hay comparable y se declara.\n- Exclusiones de ruido (aplican al Calor y al KPI \"ficheros tocados\"): `package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, `Cargo.lock`, `poetry.lock`, `Gemfile.lock`, `go.sum`, `*.min.js`, `*.map`, rutas bajo `dist/`, `build/`, `vendor/`.\n- Carpeta principal: default `src/` si existe (raíz si no); acota SOLO el Calor y sus %; persistida por repo en un JSON de datos del servidor; si la guardada ya no existe, fallback a la automática con aviso.\n- Raíz de clones configurable (default `~/git`); se escanean solo hijos directos; `id` de repo = nombre de su carpeta.\n- Renames: no se siguen (un rename es un path nuevo). Caché en memoria por (repo, ventana, sha de HEAD). Sin base de datos.\n- Foto desactualizada: mtime de `.git/FETCH_HEAD`; banner si >7 días; sin `FETCH_HEAD`, ni fecha ni banner.\n- Textos de UI en español, como la maqueta.\n- Tests del análisis y de la API contra repos fixture creados en tmp (`git init` + commits con `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE` fijadas); nunca contra repos reales de la máquina.\n\n## Contexto heredado\n_(vacía — la rellena la sesión coordinadora cuando algo ya mergeado condiciona a este slice. `/ct-groom` no escribe aquí ni reescribe lo que escribas.)_\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- una ventana con 0 commits enseña \"0 commits en X\" con CTA a 12 meses\n- una carpeta sin .git enseña el estado diseñado con la lista de clones\n- un clon con FETCH_HEAD de más de 7 días enseña el banner de desactualizado\n- un clon sin FETCH_HEAD no enseña ni fecha de traída ni banner\n\n## Dependencias\n*(cada `#N` de esta sección es el ORDEN del slice en la tabla §9 del spec, NO un número de issue de GitHub — `/ct-next` lo traduce por el marcador `ct-order` de cada issue)*\n- merge-after `#6`\n\n## Gates\nAntes de mergear, estos gates los cierra un HUMANO — el agente que implementa el slice no puede darlos por cumplidos:\n- **`visual`** — antes de mergear, un humano tiene que VER el cambio: el PR debe traer captura (o vídeo) del antes/después y la ruta para reproducirlo. El agente no puede darlo por cumplido.\n- **`plan`** — antes de implementar, un humano tiene que revisar el PLAN del slice: el agente lo publica como comentario de este issue y se detiene hasta el OK. El agente no puede darlo por cumplido.\n\n## Out of scope / Protected\n- 🚫 Sin botón \"Traer cambios\", sin progreso por pasos, sin \"Elegir otra carpeta…\"\n\n<!-- ct-order:7 -->","state":"OPEN","stateReason":null,"milestone":{"number":1,"title":"Repo Pulse MVP","description":null},"labels":{"nodes":[{"name":"gate:plan"},{"name":"status:backlog"},{"name":"type:ui"},{"name":"area:web"},{"name":"gate:visual"}]}},{"number":21,"url":"https://github.com/jjponz/repo-pulse/issues/21","title":"ct-ledger — XOP-4909","body":"Registro de decisiones del epic **XOP-4909**.\n\nCada puerta (congelar / promover / merge) añade aquí un comentario\ninmutable con su veredicto. Este issue es el ledger append-only del\nepic: no es un slice y no lleva `ct-order`.","state":"OPEN","stateReason":null,"milestone":{"number":2,"title":"XOP-4909","description":null},"labels":{"nodes":[{"name":"ct-ledger"}]}},{"number":22,"url":"https://github.com/jjponz/repo-pulse/issues/22","title":"#1 carga indeterminada","body":"> Slice `#1` del epic. Spec: [docs/superpowers/specs/XOP-4909.md § Tabla de slices](https://github.com/jjponz/repo-pulse/blob/main/docs/superpowers/specs/XOP-4909.md)\n\nREPO: jjponz/repo-pulse\nINTENCION: Cada estado no feliz del dashboard dice qué pasa y ofrece la salida, sobre las vistas ya mergeadas de #5 y #6. Los estados son cinco: carga indeterminada, ventana sin actividad, clon sin commits, carpeta sin `.git` y foto local vieja.\nSENAL: Un estado no feliz deja la vista en blanco, un indicador de carga que nunca termina o un código de error crudo. El desarrollador no sabe qué le pasa ni qué hacer, así que recarga la página o abandona la herramienta.\n\n## Descripción\nen `web/src/App.tsx`: sustituye el texto plano `Cargando…` por un único indicador indeterminado con `role=\"status\"` que ocupa el sitio de la rejilla mientras `summary` es null\n\n## Contexto del epic\nStack: Node ≥22, TypeScript estricto; npm workspaces `web/` (Vite+React) y `server/` (Express); tests con Vitest.\nEste slice toca SOLO `web/`. No cambia nada bajo `server/`.\n`web/` nunca importa de `server/`: los tipos de payload viven en `web/src/api/types.ts` y se sincronizan a mano.\nLos nombres de autores no salen NUNCA del servidor ni aparecen en el DOM: solo conteos y porcentajes.\nVentanas: `30d`, `90d`, `12m` (default), `all`. Sus etiquetas de UI salen de `windowLabel` en `web/src/format.ts`: \"30 días\", \"90 días\", \"12 meses\", \"todo\".\nSeñales que este slice consume del payload: `headSha` (null = clon sin commits), los cubos de `buckets`, `meta.stale`, `meta.fetchedAt` (null = sin FETCH_HEAD) y el código de error `not-a-git-repo`.\n`meta.stale` lo calcula el servidor con el mtime de `.git/FETCH_HEAD` y el umbral de 7 días. La UI no recalcula el umbral: lee el booleano.\nEstado de partida: `web/src/App.tsx` pinta hoy el texto plano `Cargando…` y una línea de error genérica. `web/src/Header.tsx` colorea la línea de foto local según `stale` pero no pinta banner. `web/src/Heat.tsx` tiene su propia línea de error.\nTextos de UI en español.\nReferencia visual: `docs/design/repo-pulse-mockup.html`. Es un bundle sin cadenas de estos estados, así que el texto exacto lo fija este spec (D-9). Los chips \"estado de demo\" del pie NO se implementan.\nTests de la UI con Vitest y Testing Library contra respuestas de API stubbeadas, como en `web/src/App.test.tsx`; nunca contra repos reales de la máquina.\nGates humanos del epic: el PR de cada fila trae captura del antes/después y la ruta para reproducirlo. El agente publica el plan del slice y espera el OK antes de implementar.\n\n## Decisiones congeladas\n- **D-1 · La señal de fallo es la vista muerta** — El slice falla si algún estado no feliz deja la vista en blanco, un indicador de carga eterno o un código de error crudo.\n- **D-2 · Tres slices, no uno ni cinco** — La US se trocea en tres filas: carga indeterminada, vacíos de datos, estados del clon.\n- **D-3 · El anti-scope suma dos puntos al del ticket** — Además de los tres puntos del ticket, el slice no toca `server/` y ningún estado reintenta ni hace polling.\n- **D-4 · El CTA escala una ventana, y desde 12m salta a `all`** — Desde `30d` y `90d` el CTA lleva a `12m`. Desde `12m` lleva a `all`. En `all` no hay CTA: no existe ventana más ancha.\n- **D-5 · `headSha` separa los dos vacíos** — `headSha: null` es el estado \"clon sin commits\", sin CTA. `headSha` no nulo con todos los cubos a 0 es el estado \"ventana sin actividad\", con CTA.\n- **D-6 · El estado no-repo deja viva la cabecera** — El código `not-a-git-repo` sustituye solo los bloques de datos. La cabecera y sus dos selectores siguen montados, y el estado lista los clones de `GET /repos` como botones.\n- **D-7 · Una sola señal de foto vieja** — El banner de `stale` va encima de la cabecera con `role=\"status\"`, y la línea de foto local de `Header.tsx` pierde su color de alerta.\n- **D-8 · La carga es un indicador único** — Un solo elemento indeterminado con `role=\"status\"` ocupa el sitio de la rejilla. Sin skeleton por bloque.\n- **D-9 · El spec fija las cadenas literales** — Los criterios de aceptación citan el texto exacto en español, reusando `windowLabel` de `web/src/format.ts` para la etiqueta de ventana.\n- **D-10 · El indicador del Calor queda independiente** — El bloque Calor carga aparte contra `GET /heat`, así que conserva su propio indicador y su propia línea de error.\n- **D-11 · La vara del repo queda fuera de este slice** — Crear `.ct-vara.md` no es trabajo de esta US. `VARA-V1` y `SPEC-V19` se resuelven fuera.\n\n## Contexto heredado\n- (sin contexto heredado)\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- con la respuesta de `GET /summary` pendiente la vista expone exactamente un elemento con `role=\"status\"` y ningún bloque de datos\n- el texto `Cargando…` no aparece en el DOM en ningún estado\n- cuando el summary llega ese elemento desaparece y los bloques Pulso/Gente/Tendencia/Calor están montados\n- el indicador del bloque Calor sigue siendo un elemento distinto del de la vista\n\n## Gates\n- visual\n\n## Out of scope / Protected\n- El indicador y la línea de error propios de `web/src/Heat.tsx` no se tocan\n\n## Fuentes de convención\n- doc: `AGENTS.md` @2ebfb4a77d738b48223c1b9df612a2991c7ed48e\n- doc: `docs/superpowers/specs/2026-08-13-repo-pulse-mvp-execution.md` @d7de4bad5184ac066dfdc83cb729c29f458bf29d\n- doc: `docs/design/repo-pulse-mockup.html` @abe21196185b5670e975f7eea4207b7943249984\n\n<!-- ct-order:1 -->\n<!-- slice-runner:estado {} -->\n","state":"OPEN","stateReason":null,"milestone":{"number":2,"title":"XOP-4909","description":null},"labels":{"nodes":[{"name":"gate:apply"},{"name":"type:ui"},{"name":"area:web"},{"name":"status:in-review"}]}},{"number":23,"url":"https://github.com/jjponz/repo-pulse/issues/23","title":"#2 vacíos de datos: ventana sin actividad y clon sin commits","body":"> Slice `#2` del epic. Spec: [docs/superpowers/specs/XOP-4909.md § Tabla de slices](https://github.com/jjponz/repo-pulse/blob/main/docs/superpowers/specs/XOP-4909.md)\n\nREPO: jjponz/repo-pulse\nINTENCION: Cada estado no feliz del dashboard dice qué pasa y ofrece la salida, sobre las vistas ya mergeadas de #5 y #6. Los estados son cinco: carga indeterminada, ventana sin actividad, clon sin commits, carpeta sin `.git` y foto local vieja.\nSENAL: Un estado no feliz deja la vista en blanco, un indicador de carga que nunca termina o un código de error crudo. El desarrollador no sabe qué le pasa ni qué hacer, así que recarga la página o abandona la herramienta.\n\n## Descripción\nsobre la vista de #1: dos estados distintos según `headSha` del summary que consume #1 — \"0 commits en <ventana>\" con CTA a la ventana más ancha\\, y \"clon sin commits\" sin CTA\n\n## Contexto del epic\nStack: Node ≥22, TypeScript estricto; npm workspaces `web/` (Vite+React) y `server/` (Express); tests con Vitest.\nEste slice toca SOLO `web/`. No cambia nada bajo `server/`.\n`web/` nunca importa de `server/`: los tipos de payload viven en `web/src/api/types.ts` y se sincronizan a mano.\nLos nombres de autores no salen NUNCA del servidor ni aparecen en el DOM: solo conteos y porcentajes.\nVentanas: `30d`, `90d`, `12m` (default), `all`. Sus etiquetas de UI salen de `windowLabel` en `web/src/format.ts`: \"30 días\", \"90 días\", \"12 meses\", \"todo\".\nSeñales que este slice consume del payload: `headSha` (null = clon sin commits), los cubos de `buckets`, `meta.stale`, `meta.fetchedAt` (null = sin FETCH_HEAD) y el código de error `not-a-git-repo`.\n`meta.stale` lo calcula el servidor con el mtime de `.git/FETCH_HEAD` y el umbral de 7 días. La UI no recalcula el umbral: lee el booleano.\nEstado de partida: `web/src/App.tsx` pinta hoy el texto plano `Cargando…` y una línea de error genérica. `web/src/Header.tsx` colorea la línea de foto local según `stale` pero no pinta banner. `web/src/Heat.tsx` tiene su propia línea de error.\nTextos de UI en español.\nReferencia visual: `docs/design/repo-pulse-mockup.html`. Es un bundle sin cadenas de estos estados, así que el texto exacto lo fija este spec (D-9). Los chips \"estado de demo\" del pie NO se implementan.\nTests de la UI con Vitest y Testing Library contra respuestas de API stubbeadas, como en `web/src/App.test.tsx`; nunca contra repos reales de la máquina.\nGates humanos del epic: el PR de cada fila trae captura del antes/después y la ruta para reproducirlo. El agente publica el plan del slice y espera el OK antes de implementar.\n\n## Decisiones congeladas\n- **D-1 · La señal de fallo es la vista muerta** — El slice falla si algún estado no feliz deja la vista en blanco, un indicador de carga eterno o un código de error crudo.\n- **D-2 · Tres slices, no uno ni cinco** — La US se trocea en tres filas: carga indeterminada, vacíos de datos, estados del clon.\n- **D-3 · El anti-scope suma dos puntos al del ticket** — Además de los tres puntos del ticket, el slice no toca `server/` y ningún estado reintenta ni hace polling.\n- **D-4 · El CTA escala una ventana, y desde 12m salta a `all`** — Desde `30d` y `90d` el CTA lleva a `12m`. Desde `12m` lleva a `all`. En `all` no hay CTA: no existe ventana más ancha.\n- **D-5 · `headSha` separa los dos vacíos** — `headSha: null` es el estado \"clon sin commits\", sin CTA. `headSha` no nulo con todos los cubos a 0 es el estado \"ventana sin actividad\", con CTA.\n- **D-6 · El estado no-repo deja viva la cabecera** — El código `not-a-git-repo` sustituye solo los bloques de datos. La cabecera y sus dos selectores siguen montados, y el estado lista los clones de `GET /repos` como botones.\n- **D-7 · Una sola señal de foto vieja** — El banner de `stale` va encima de la cabecera con `role=\"status\"`, y la línea de foto local de `Header.tsx` pierde su color de alerta.\n- **D-8 · La carga es un indicador único** — Un solo elemento indeterminado con `role=\"status\"` ocupa el sitio de la rejilla. Sin skeleton por bloque.\n- **D-9 · El spec fija las cadenas literales** — Los criterios de aceptación citan el texto exacto en español, reusando `windowLabel` de `web/src/format.ts` para la etiqueta de ventana.\n- **D-10 · El indicador del Calor queda independiente** — El bloque Calor carga aparte contra `GET /heat`, así que conserva su propio indicador y su propia línea de error.\n- **D-11 · La vara del repo queda fuera de este slice** — Crear `.ct-vara.md` no es trabajo de esta US. `VARA-V1` y `SPEC-V19` se resuelven fuera.\n\n## Contexto heredado\n- (sin contexto heredado)\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- con `headSha` no nulo y todos los cubos a 0 la vista enseña el texto `0 commits en 90 días` en la ventana `90d` y `0 commits en 30 días` en la ventana `30d`\n- ese estado ofrece un botón `Ver 12 meses` en las ventanas `30d` y `90d` y un botón `Ver todo` en la ventana `12m`\n- en la ventana `all` con 0 commits el estado vacío no ofrece ningún botón de cambio de ventana\n- pulsar ese botón cambia la ventana activa y relanza `GET /summary` sin recargar la página\n- con `headSha` null la vista enseña el texto `Este clon todavía no tiene commits` y ningún botón de cambio de ventana en las cuatro ventanas\n\n## Dependencias\n- merge-after `#1`\n\n## Gates\n- visual\n\n## Out of scope / Protected\n- Sin progreso por pasos\n\n## Fuentes de convención\n- doc: `AGENTS.md` @2ebfb4a77d738b48223c1b9df612a2991c7ed48e\n- doc: `docs/superpowers/specs/2026-08-13-repo-pulse-mvp-execution.md` @d7de4bad5184ac066dfdc83cb729c29f458bf29d\n- doc: `docs/design/repo-pulse-mockup.html` @abe21196185b5670e975f7eea4207b7943249984\n\n<!-- ct-order:2 -->\n<!-- slice-runner:estado {} -->\n","state":"OPEN","stateReason":null,"milestone":{"number":2,"title":"XOP-4909","description":null},"labels":{"nodes":[{"name":"type:ui"},{"name":"area:web"},{"name":"gate:visual"},{"name":"status:in-review"}]}},{"number":28,"url":"https://github.com/jjponz/repo-pulse/issues/28","title":"#3 estados del clon: no-repo y foto desactualizada","body":"> Slice `#3` del epic. Spec: [docs/superpowers/specs/XOP-4909.md § Tabla de slices](https://github.com/jjponz/repo-pulse/blob/main/docs/superpowers/specs/XOP-4909.md)\n\nREPO: jjponz/repo-pulse\nINTENCION: Cada estado no feliz del dashboard dice qué pasa y ofrece la salida, sobre las vistas ya mergeadas de #5 y #6. Los estados son cinco: carga indeterminada, ventana sin actividad, clon sin commits, carpeta sin `.git` y foto local vieja.\nSENAL: Un estado no feliz deja la vista en blanco, un indicador de carga que nunca termina o un código de error crudo. El desarrollador no sabe qué le pasa ni qué hacer, así que recarga la página o abandona la herramienta.\n\n## Descripción\nsobre la vista de #2: el código `not-a-git-repo` de `GET /summary` sustituye los bloques de datos por un estado que lista los clones de `GET /repos` como botones\\, y un banner con `role=\"status\"` encima de la cabecera cuando `meta.stale` es true\n\n## Contexto del epic\nStack: Node ≥22, TypeScript estricto; npm workspaces `web/` (Vite+React) y `server/` (Express); tests con Vitest.\nEste slice toca SOLO `web/`. No cambia nada bajo `server/`.\n`web/` nunca importa de `server/`: los tipos de payload viven en `web/src/api/types.ts` y se sincronizan a mano.\nLos nombres de autores no salen NUNCA del servidor ni aparecen en el DOM: solo conteos y porcentajes.\nVentanas: `30d`, `90d`, `12m` (default), `all`. Sus etiquetas de UI salen de `windowLabel` en `web/src/format.ts`: \"30 días\", \"90 días\", \"12 meses\", \"todo\".\nSeñales que este slice consume del payload: `headSha` (null = clon sin commits), los cubos de `buckets`, `meta.stale`, `meta.fetchedAt` (null = sin FETCH_HEAD) y el código de error `not-a-git-repo`.\n`meta.stale` lo calcula el servidor con el mtime de `.git/FETCH_HEAD` y el umbral de 7 días. La UI no recalcula el umbral: lee el booleano.\nEstado de partida: `web/src/App.tsx` pinta hoy el texto plano `Cargando…` y una línea de error genérica. `web/src/Header.tsx` colorea la línea de foto local según `stale` pero no pinta banner. `web/src/Heat.tsx` tiene su propia línea de error.\nTextos de UI en español.\nReferencia visual: `docs/design/repo-pulse-mockup.html`. Es un bundle sin cadenas de estos estados, así que el texto exacto lo fija este spec (D-9). Los chips \"estado de demo\" del pie NO se implementan.\nTests de la UI con Vitest y Testing Library contra respuestas de API stubbeadas, como en `web/src/App.test.tsx`; nunca contra repos reales de la máquina.\nGates humanos del epic: el PR de cada fila trae captura del antes/después y la ruta para reproducirlo. El agente publica el plan del slice y espera el OK antes de implementar.\n\n## Decisiones congeladas\n- **D-1 · La señal de fallo es la vista muerta** — El slice falla si algún estado no feliz deja la vista en blanco, un indicador de carga eterno o un código de error crudo.\n- **D-2 · Tres slices, no uno ni cinco** — La US se trocea en tres filas: carga indeterminada, vacíos de datos, estados del clon.\n- **D-3 · El anti-scope suma dos puntos al del ticket** — Además de los tres puntos del ticket, el slice no toca `server/` y ningún estado reintenta ni hace polling.\n- **D-4 · El CTA escala una ventana, y desde 12m salta a `all`** — Desde `30d` y `90d` el CTA lleva a `12m`. Desde `12m` lleva a `all`. En `all` no hay CTA: no existe ventana más ancha.\n- **D-5 · `headSha` separa los dos vacíos** — `headSha: null` es el estado \"clon sin commits\", sin CTA. `headSha` no nulo con todos los cubos a 0 es el estado \"ventana sin actividad\", con CTA.\n- **D-6 · El estado no-repo deja viva la cabecera** — El código `not-a-git-repo` sustituye solo los bloques de datos. La cabecera y sus dos selectores siguen montados, y el estado lista los clones de `GET /repos` como botones.\n- **D-7 · Una sola señal de foto vieja** — El banner de `stale` va encima de la cabecera con `role=\"status\"`, y la línea de foto local de `Header.tsx` pierde su color de alerta.\n- **D-8 · La carga es un indicador único** — Un solo elemento indeterminado con `role=\"status\"` ocupa el sitio de la rejilla. Sin skeleton por bloque.\n- **D-9 · El spec fija las cadenas literales** — Los criterios de aceptación citan el texto exacto en español, reusando `windowLabel` de `web/src/format.ts` para la etiqueta de ventana.\n- **D-10 · El indicador del Calor queda independiente** — El bloque Calor carga aparte contra `GET /heat`, así que conserva su propio indicador y su propia línea de error.\n- **D-11 · La vara del repo queda fuera de este slice** — Crear `.ct-vara.md` no es trabajo de esta US. `VARA-V1` y `SPEC-V19` se resuelven fuera.\n\n## Contexto heredado\n- (sin contexto heredado)\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- con `GET /summary` respondiendo el código `not-a-git-repo` la vista enseña el texto `Esta carpeta no es un repositorio git` y ningún bloque de datos\n- ese estado lista un botón por cada clon de `GET /repos` y pulsar uno cambia el repo seleccionado sin recargar la página\n- la cabecera con el selector de repositorio y el de ventana sigue montada durante el estado no-repo\n- con `meta.stale` true existe encima de la cabecera un elemento con `role=\"status\"` cuyo texto nombra los días transcurridos desde `meta.fetchedAt`\n- con `meta.stale` false ese elemento no existe y la línea de foto local dice que está al día\n- con `meta.fetchedAt` null no existe ni el banner ni la línea de foto local\n- la línea de foto local de `web/src/Header.tsx` no usa el color de alerta en ningún caso\n\n## Dependencias\n- merge-after `#2`\n\n## Gates\n- visual\n\n## Out of scope / Protected\n- Sin botón \"Traer cambios\" y sin \"Elegir otra carpeta…\"\n\n## Fuentes de convención\n- doc: `AGENTS.md` @2ebfb4a77d738b48223c1b9df612a2991c7ed48e\n- doc: `docs/superpowers/specs/2026-08-13-repo-pulse-mvp-execution.md` @d7de4bad5184ac066dfdc83cb729c29f458bf29d\n- doc: `docs/design/repo-pulse-mockup.html` @abe21196185b5670e975f7eea4207b7943249984\n\n<!-- ct-order:3 -->\n<!-- slice-runner:estado {} -->\n","state":"OPEN","stateReason":null,"milestone":{"number":2,"title":"XOP-4909","description":null},"labels":{"nodes":[{"name":"status:backlog"},{"name":"type:ui"},{"name":"area:web"},{"name":"gate:visual"}]}},{"number":42,"url":"https://github.com/jjponz/repo-pulse/issues/42","title":"XOP-4909 la métrica de los campeones","body":"> Historia de usuario: XOP-4909\n> Para pedir cambios en el plan, comenta en este issue empezando por `-REVIEW`: lo que escribas detrás es lo que se le pide al agente, y publicará el plan rehecho aquí mismo.\n\n## Descripción\nla métrica de los campeones\n\n## Contexto del epic\nDescripción\nquiero tener la metrica de los campeones: qué code coverage tiene los repositorios que estamos analizando\nDefinition of Done\nContexto: repo-pulse es un dashboard local de salud de repos git (bloques Pulso, Gente y Calor). El servidor server/ (Express + TS) solo LEE los clones que cuelgan de REPO_PULSE_ROOT (por defecto ~/git, sin recursión) y nunca los muta ni ejecuta nada en ellos salvo git. La UI web/ (Vite + React) consume /api. Esta historia añade la cobertura de tests de cada clon como una métrica más, con su bloque «Campeones»: el ranking de los clones por cobertura.\nDe dónde sale la cobertura (regla de oro: leer, nunca ejecutar):\nLa cobertura se lee de los artefactos de cobertura que YA existan en el clon. El servidor NO ejecuta tests, NO ejecuta herramientas de cobertura, NO hace fetch ni consulta servicios externos (CI, Codecov, SonarQube…).\n\nFormatos mínimos que hay que entender: lcov.info (lcov), coverage-summary.json (Istanbul: Vitest/Jest/nyc) y coverage.xml (Cobertura: pytest-cov, Go, .NET). JaCoCo (jacocoTestReport.xml) es deseable pero no bloquea. El plan cierra la lista final, las rutas donde se buscan y el orden de preferencia cuando un clon trae varios.\n\nLa métrica es el porcentaje de LÍNEAS cubiertas (covered / total, redondeado a un decimal). Si el formato no trae líneas, ese artefacto no cuenta.\n\nCuando un clon no trae ningún artefacto, la cobertura es null y la UI dice «Sin datos de cobertura». Nunca se inventa un 0 %.\n\nLa fecha de la medida es el mtime del artefacto leído, y se expone y se pinta: una cobertura de hace tres meses no puede parecer de hoy.\n\nAPI (misma casa que los cuatro endpoints existentes):\nUn endpoint nuevo bajo /api devuelve la cobertura de TODOS los clones del root, ordenada de mayor a menor cobertura y con los null al final: por clon, id, percentage (número o null), lines: { covered, total } (o null), source (formato leído, o null) y measuredAt (ISO 8601, o null). El plan fija la ruta exacta.\n\nErrores con el sobre tipado ya existente { error: { code, message } }; ninguna respuesta lleva nombres ni emails de autores (aquí no hacen falta y no deben aparecer).\n\nCaché por (clon, mtime del artefacto) siguiendo el patrón de server/src/api/routes.ts: parsear un lcov.info grande no debe repetirse en cada petición si el artefacto no ha cambiado.\n\nEl servidor sigue escuchando solo en 127.0.0.1 y web/ sigue sin importar tipos de server/: los tipos del payload se declaran en web/src/api/types.ts.\n\nUI (gate visual: captura antes/después en el PR):\nUn bloque nuevo «Campeones» en el dashboard, junto a Pulso, Gente y Calor, con el ranking de los clones por cobertura: nombre, porcentaje con barra, formato de origen y antigüedad de la medida. El clon seleccionado en la cabecera se resalta en el ranking.\n\nEstados no felices visibles: clon sin artefacto («Sin datos de cobertura»), ningún clon con datos (el bloque lo dice en vez de quedarse vacío), error de la API (mismo tratamiento que los demás bloques).\n\nTextos visibles en castellano; tokens y componentes del design system ya presentes en web/src/tokens.css.\n\nCalidad:\nCódigo, identificadores, comentarios y nombres de test en inglés (AGENTS.md). Tests Vitest junto al código: un fixture por formato soportado, más los casos «sin artefacto», «artefacto corrupto» y «artefacto sin líneas».\n\nnpm run build, npm test y npm run lint en verde desde la raíz; la CI del PR en verde.\n\nFuera de alcance (protegido):\nEjecutar tests o herramientas en el clon; leer cobertura de CI o servicios externos; histórico o tendencia de la cobertura; cobertura por carpeta o fichero (eso sería Calor); umbrales, alertas o colores «semáforo» configurables.\n\nGates\nAntes de mergear, estos gates los cierra un HUMANO — el agente que implementa el slice no puede darlos por cumplidos:\nvisual — antes de mergear, un humano tiene que VER el cambio: el PR debe traer captura (o vídeo) del antes/después y la ruta para reproducirlo. El agente no puede darlo por cumplido.\n\nplan — antes de implementar, un humano tiene que revisar el PLAN del slice: el agente lo publica como comentario de este issue y se detiene hasta el OK. El agente no puede darlo por cumplido.\n\n## Contexto heredado\n_(vacía — la rellena la sesión coordinadora cuando algo ya mergeado condiciona a este slice. `/ct-groom` no escribe aquí ni reescribe lo que escribas.)_\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- (rellenar desde el spec)\n\n## Gates\nAntes de mergear, estos gates los cierra un HUMANO — el agente que implementa el slice no puede darlos por cumplidos:\n- **`plan`** — antes de implementar, un humano tiene que revisar el PLAN del slice: el agente lo publica como comentario de este issue y se detiene. Para darle el go, contesta con un comentario que sea exactamente `-OK <nonce>`, con el nonce que /ct-next imprimió al despachar este slice (sin nada más: cualquier otra cosa no arranca nada, a propósito). Ese nonce no está escrito en este issue porque el agente lee el issue: es la parte del permiso que él no puede fabricar, y sin él `--release` se niega. Si se ha perdido, quien despachó lo reemite con `scripts/ct-go.mjs`. Quien vigila el issue es un proceso que la coordinadora lanzó al despachar y que espera unas horas: si contestas mucho más tarde puede haber caducado, y entonces hay que empujar la sesión a mano. El agente no puede darlo por cumplido.\n\n## Out of scope / Protected\n- (ninguno declarado)\n","state":"OPEN","stateReason":null,"milestone":null,"labels":{"nodes":[{"name":"gate:plan"},{"name":"status:in-progress"}]}},{"number":43,"url":"https://github.com/jjponz/repo-pulse/issues/43","title":"XOP-4909 la métrica de los campeones","body":"> Historia de usuario: XOP-4909\n> Para pedir cambios en el plan, comenta en este issue empezando por `-REVIEW`: lo que escribas detrás es lo que se le pide al agente, y publicará el plan rehecho aquí mismo.\n\n## Descripción\nla métrica de los campeones\n\n## Contexto del epic\nDescripción\nquiero tener la metrica de los campeones: qué code coverage tiene los repositorios que estamos analizando\nDefinition of Done\nContexto: repo-pulse es un dashboard local de salud de repos git (bloques Pulso, Gente y Calor). El servidor server/ (Express + TS) solo LEE los clones que cuelgan de REPO_PULSE_ROOT (por defecto ~/git, sin recursión) y nunca los muta ni ejecuta nada en ellos salvo git. La UI web/ (Vite + React) consume /api. Esta historia añade la cobertura de tests de cada clon como una métrica más, con su bloque «Campeones»: el ranking de los clones por cobertura.\nDe dónde sale la cobertura (regla de oro: leer, nunca ejecutar):\nLa cobertura se lee de los artefactos de cobertura que YA existan en el clon. El servidor NO ejecuta tests, NO ejecuta herramientas de cobertura, NO hace fetch ni consulta servicios externos (CI, Codecov, SonarQube…).\n\nFormatos mínimos que hay que entender: lcov.info (lcov), coverage-summary.json (Istanbul: Vitest/Jest/nyc) y coverage.xml (Cobertura: pytest-cov, Go, .NET). JaCoCo (jacocoTestReport.xml) es deseable pero no bloquea. El plan cierra la lista final, las rutas donde se buscan y el orden de preferencia cuando un clon trae varios.\n\nLa métrica es el porcentaje de LÍNEAS cubiertas (covered / total, redondeado a un decimal). Si el formato no trae líneas, ese artefacto no cuenta.\n\nCuando un clon no trae ningún artefacto, la cobertura es null y la UI dice «Sin datos de cobertura». Nunca se inventa un 0 %.\n\nLa fecha de la medida es el mtime del artefacto leído, y se expone y se pinta: una cobertura de hace tres meses no puede parecer de hoy.\n\nAPI (misma casa que los cuatro endpoints existentes):\nUn endpoint nuevo bajo /api devuelve la cobertura de TODOS los clones del root, ordenada de mayor a menor cobertura y con los null al final: por clon, id, percentage (número o null), lines: { covered, total } (o null), source (formato leído, o null) y measuredAt (ISO 8601, o null). El plan fija la ruta exacta.\n\nErrores con el sobre tipado ya existente { error: { code, message } }; ninguna respuesta lleva nombres ni emails de autores (aquí no hacen falta y no deben aparecer).\n\nCaché por (clon, mtime del artefacto) siguiendo el patrón de server/src/api/routes.ts: parsear un lcov.info grande no debe repetirse en cada petición si el artefacto no ha cambiado.\n\nEl servidor sigue escuchando solo en 127.0.0.1 y web/ sigue sin importar tipos de server/: los tipos del payload se declaran en web/src/api/types.ts.\n\nUI (gate visual: captura antes/después en el PR):\nUn bloque nuevo «Campeones» en el dashboard, junto a Pulso, Gente y Calor, con el ranking de los clones por cobertura: nombre, porcentaje con barra, formato de origen y antigüedad de la medida. El clon seleccionado en la cabecera se resalta en el ranking.\n\nEstados no felices visibles: clon sin artefacto («Sin datos de cobertura»), ningún clon con datos (el bloque lo dice en vez de quedarse vacío), error de la API (mismo tratamiento que los demás bloques).\n\nTextos visibles en castellano; tokens y componentes del design system ya presentes en web/src/tokens.css.\n\nCalidad:\nCódigo, identificadores, comentarios y nombres de test en inglés (AGENTS.md). Tests Vitest junto al código: un fixture por formato soportado, más los casos «sin artefacto», «artefacto corrupto» y «artefacto sin líneas».\n\nnpm run build, npm test y npm run lint en verde desde la raíz; la CI del PR en verde.\n\nFuera de alcance (protegido):\nEjecutar tests o herramientas en el clon; leer cobertura de CI o servicios externos; histórico o tendencia de la cobertura; cobertura por carpeta o fichero (eso sería Calor); umbrales, alertas o colores «semáforo» configurables.\n\nGates\nAntes de mergear, estos gates los cierra un HUMANO — el agente que implementa el slice no puede darlos por cumplidos:\nvisual — antes de mergear, un humano tiene que VER el cambio: el PR debe traer captura (o vídeo) del antes/después y la ruta para reproducirlo. El agente no puede darlo por cumplido.\n\nplan — antes de implementar, un humano tiene que revisar el PLAN del slice: el agente lo publica como comentario de este issue y se detiene hasta el OK. El agente no puede darlo por cumplido.\n\n## Contexto heredado\n_(vacía — la rellena la sesión coordinadora cuando algo ya mergeado condiciona a este slice. `/ct-groom` no escribe aquí ni reescribe lo que escribas.)_\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- (rellenar desde el spec)\n\n## Gates\nAntes de mergear, estos gates los cierra un HUMANO — el agente que implementa el slice no puede darlos por cumplidos:\n- **`plan`** — antes de implementar, un humano tiene que revisar el PLAN del slice: el agente lo publica como comentario de este issue y se detiene. Para darle el go, contesta con un comentario que sea exactamente `-OK <nonce>`, con el nonce que /ct-next imprimió al despachar este slice (sin nada más: cualquier otra cosa no arranca nada, a propósito). Ese nonce no está escrito en este issue porque el agente lee el issue: es la parte del permiso que él no puede fabricar, y sin él `--release` se niega. Si se ha perdido, quien despachó lo reemite con `scripts/ct-go.mjs`. Quien vigila el issue es un proceso que la coordinadora lanzó al despachar y que espera unas horas: si contestas mucho más tarde puede haber caducado, y entonces hay que empujar la sesión a mano. El agente no puede darlo por cumplido.\n\n## Out of scope / Protected\n- (ninguno declarado)\n","state":"OPEN","stateReason":null,"milestone":null,"labels":{"nodes":[{"name":"gate:plan"},{"name":"status:ready"}]}},{"number":50,"url":"https://github.com/jjponz/repo-pulse/issues/50","title":"XOP-4909 la métrica de los campeones","body":"> Historia de usuario: XOP-4909\n> Para pedir cambios en el plan, comenta en este issue empezando por `-REVIEW`: lo que escribas detrás es lo que se le pide al agente, y publicará el plan rehecho aquí mismo.\n\n## Descripción\nla métrica de los campeones\n\n## Contexto del epic\nDescripción\nquiero tener la metrica de los campeones: qué code coverage tiene los repositorios que estamos analizando\nDefinition of Done\nContexto: repo-pulse es un dashboard local de salud de repos git (bloques Pulso, Gente y Calor). El servidor server/ (Express + TS) solo LEE los clones que cuelgan de REPO_PULSE_ROOT (por defecto ~/git, sin recursión) y nunca los muta ni ejecuta nada en ellos salvo git. La UI web/ (Vite + React) consume /api. Esta historia añade la cobertura de tests de cada clon como una métrica más, con su bloque «Campeones»: el ranking de los clones por cobertura.\nDe dónde sale la cobertura (regla de oro: leer, nunca ejecutar):\nLa cobertura se lee de los artefactos de cobertura que YA existan en el clon. El servidor NO ejecuta tests, NO ejecuta herramientas de cobertura, NO hace fetch ni consulta servicios externos (CI, Codecov, SonarQube…).\n\nFormatos mínimos que hay que entender: lcov.info (lcov), coverage-summary.json (Istanbul: Vitest/Jest/nyc) y coverage.xml (Cobertura: pytest-cov, Go, .NET). JaCoCo (jacocoTestReport.xml) es deseable pero no bloquea. El plan cierra la lista final, las rutas donde se buscan y el orden de preferencia cuando un clon trae varios.\n\nLa métrica es el porcentaje de LÍNEAS cubiertas (covered / total, redondeado a un decimal). Si el formato no trae líneas, ese artefacto no cuenta.\n\nCuando un clon no trae ningún artefacto, la cobertura es null y la UI dice «Sin datos de cobertura». Nunca se inventa un 0 %.\n\nLa fecha de la medida es el mtime del artefacto leído, y se expone y se pinta: una cobertura de hace tres meses no puede parecer de hoy.\n\nAPI (misma casa que los cuatro endpoints existentes):\nUn endpoint nuevo bajo /api devuelve la cobertura de TODOS los clones del root, ordenada de mayor a menor cobertura y con los null al final: por clon, id, percentage (número o null), lines: { covered, total } (o null), source (formato leído, o null) y measuredAt (ISO 8601, o null). El plan fija la ruta exacta.\n\nErrores con el sobre tipado ya existente { error: { code, message } }; ninguna respuesta lleva nombres ni emails de autores (aquí no hacen falta y no deben aparecer).\n\nCaché por (clon, mtime del artefacto) siguiendo el patrón de server/src/api/routes.ts: parsear un lcov.info grande no debe repetirse en cada petición si el artefacto no ha cambiado.\n\nEl servidor sigue escuchando solo en 127.0.0.1 y web/ sigue sin importar tipos de server/: los tipos del payload se declaran en web/src/api/types.ts.\n\nUI (gate visual: captura antes/después en el PR):\nUn bloque nuevo «Campeones» en el dashboard, junto a Pulso, Gente y Calor, con el ranking de los clones por cobertura: nombre, porcentaje con barra, formato de origen y antigüedad de la medida. El clon seleccionado en la cabecera se resalta en el ranking.\n\nEstados no felices visibles: clon sin artefacto («Sin datos de cobertura»), ningún clon con datos (el bloque lo dice en vez de quedarse vacío), error de la API (mismo tratamiento que los demás bloques).\n\nTextos visibles en castellano; tokens y componentes del design system ya presentes en web/src/tokens.css.\n\nCalidad:\nCódigo, identificadores, comentarios y nombres de test en inglés (AGENTS.md). Tests Vitest junto al código: un fixture por formato soportado, más los casos «sin artefacto», «artefacto corrupto» y «artefacto sin líneas».\n\nnpm run build, npm test y npm run lint en verde desde la raíz; la CI del PR en verde.\n\nFuera de alcance (protegido):\nEjecutar tests o herramientas en el clon; leer cobertura de CI o servicios externos; histórico o tendencia de la cobertura; cobertura por carpeta o fichero (eso sería Calor); umbrales, alertas o colores «semáforo» configurables.\n\nGates\nAntes de mergear, estos gates los cierra un HUMANO — el agente que implementa el slice no puede darlos por cumplidos:\nvisual — antes de mergear, un humano tiene que VER el cambio: el PR debe traer captura (o vídeo) del antes/después y la ruta para reproducirlo. El agente no puede darlo por cumplido.\n\nplan — antes de implementar, un humano tiene que revisar el PLAN del slice: el agente lo publica como comentario de este issue y se detiene hasta el OK. El agente no puede darlo por cumplido.\n\n## Contexto heredado\n_(vacía — la rellena la sesión coordinadora cuando algo ya mergeado condiciona a este slice. `/ct-groom` no escribe aquí ni reescribe lo que escribas.)_\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- (rellenar desde el spec)\n\n## Gates\nAntes de mergear, estos gates los cierra un HUMANO — el agente que implementa el slice no puede darlos por cumplidos:\n- **`plan`** — antes de implementar, un humano tiene que revisar el PLAN del slice: el agente lo publica como comentario de este issue y se detiene. Para darle el go, contesta con un comentario que sea exactamente `-OK <nonce>`, con el nonce que /ct-next imprimió al despachar este slice (sin nada más: cualquier otra cosa no arranca nada, a propósito). Ese nonce no está escrito en este issue porque el agente lee el issue: es la parte del permiso que él no puede fabricar, y sin él `--release` se niega. Si se ha perdido, quien despachó lo reemite con `scripts/ct-go.mjs`. Quien vigila el issue es un proceso que la coordinadora lanzó al despachar y que espera unas horas: si contestas mucho más tarde puede haber caducado, y entonces hay que empujar la sesión a mano. El agente no puede darlo por cumplido.\n\n## Out of scope / Protected\n- (ninguno declarado)\n","state":"OPEN","stateReason":null,"milestone":null,"labels":{"nodes":[{"name":"gate:plan"},{"name":"status:in-progress"}]}},{"number":51,"url":"https://github.com/jjponz/repo-pulse/issues/51","title":"Acoplamiento temporal: ver qué partes del repo cambian juntas Como resp…","body":"> Plan pedido a mano: no hay historia de usuario en Jira.\n> Para pedir cambios en el plan, comenta en este issue empezando por `-REVIEW`: lo que escribas detrás es lo que se le pide al agente, y publicará el plan rehecho aquí mismo.\n\n## Descripción\nAcoplamiento temporal: ver qué partes del repo cambian juntas Como resp…\n\n## Comentario de quien pide el plan\nAcoplamiento temporal: ver qué partes del repo cambian juntas Como responsable de un repo que miro en repo-pulse, quiero ver, en la ventana de tiempo que tenga elegida, qué carpetas o ficheros de la carpeta principal cambian juntos en los mismos commits y con qué fuerza, para descubrir las dependencias que la estructura de carpetas no enseña (deuda arquitectónica, copy-paste, cambios en cascada), como propone \"Your Code as a Crime Scene\" de Adam Tornhill. Calor ya me dice dónde se cambia; esto me dice qué cambia junto.  Criterios de aceptación: - Dentro de Calor, en el mismo nivel que estoy viendo, veo las parejas de hijos (carpetas o ficheros) que cambian juntas, ordenadas de mayor a menor acoplamiento. - De cada pareja veo cuántas veces han cambiado juntas y el grado de acoplamiento en porcentaje: de las veces que cambia la que menos cambia de las dos, en cuántas cambia también la otra. - Las parejas que han coincidido muy pocas veces no aparecen: hay un mínimo de coincidencias para que una pareja cuente, y ese mínimo se ve en pantalla para que sepa por qué algo no sale. - Al cambiar la ventana (30 días, 90 días, 12 meses, todo) el acoplamiento se recalcula con esa ventana, igual que el resto de Calor. - Si en la ventana no hay ninguna pareja que llegue al mínimo, la pantalla lo dice con claridad en vez de quedarse vacía. - Los commits de merge y los ficheros generados o de ruido no cuentan, igual que en el resto de análisis. - No aparece ningún nombre ni email de autor en ningún sitio: solo cuentas y porcentajes. - El servidor solo lee el clon; no lo modifica. - Lo que el servidor devuelve es lo mismo que enseña la pantalla, y si el clon no ha cambiado no se recalcula, como ya pasa con Calor. - El texto visible sigue en castellano y el vocabulario es el del producto (Pulso, Gente, Calor).  Fuera de alcance: acoplamiento entre repos distintos; hotspots por complejidad; la evolución del acoplamiento en el tiempo; cambiar nada de Pulso o Gente; una navegación distinta de la que ya tiene Calor.\n\n## Contexto del epic\n_El plan no viene de una historia de usuario de Jira._\n\n## Contexto heredado\n_(vacía — la rellena la sesión coordinadora cuando algo ya mergeado condiciona a este slice. `/ct-groom` no escribe aquí ni reescribe lo que escribas.)_\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- (rellenar desde el spec)\n\n## Gates\nAntes de mergear, estos gates los cierra un HUMANO — el agente que implementa el slice no puede darlos por cumplidos:\n- **`plan`** — antes de implementar, un humano tiene que revisar el PLAN del slice: el agente lo publica como comentario de este issue y se detiene. Para darle el go, contesta con un comentario que sea exactamente `-OK <nonce>`, con el nonce que /ct-next imprimió al despachar este slice (sin nada más: cualquier otra cosa no arranca nada, a propósito). Ese nonce no está escrito en este issue porque el agente lee el issue: es la parte del permiso que él no puede fabricar, y sin él `--release` se niega. Si se ha perdido, quien despachó lo reemite con `scripts/ct-go.mjs`. Quien vigila el issue es un proceso que la coordinadora lanzó al despachar y que espera unas horas: si contestas mucho más tarde puede haber caducado, y entonces hay que empujar la sesión a mano. El agente no puede darlo por cumplido.\n\n## Out of scope / Protected\n- (ninguno declarado)\n","state":"OPEN","stateReason":null,"milestone":null,"labels":{"nodes":[{"name":"gate:plan"},{"name":"status:in-review"}]}},{"number":55,"url":"https://github.com/jjponz/repo-pulse/issues/55","title":"CT-138 smoke A: document local-checks","body":"> Plan asked for by hand: there is no ticket behind it.\n> To ask for changes to the plan, comment on this issue starting with `-REVIEW`: whatever you write after it is what gets asked of the agent, and it will publish the redone plan right here.\n\n## Descripción\nCT-138 smoke A: document local-checks\n\n## Comentario de quien pide el plan\nCT-138 smoke A: document local-checks\nThis is a disposable end-to-end verification of Control Tower, using this repository with its owner permission. Create only docs/verification/ct-138-local-checks.md, in Spanish as required for this repository documentation. Document the Node.js requirement and the exact installation, build, test and lint commands from the root package.json and AGENTS.md. Keep the plan to one small documentation task. Do not change application code, configuration, existing tests, README.md or AGENTS.md. Follow the normal plan-review and implementation flow; stop after publishing the plan until the normal authorization arrives. Never merge the resulting pull request.\n\n## Contexto del epic\n_This plan does not come from any ticket._\n\n## Contexto heredado\n_(empty — the coordinating session fills it in when something already merged conditions this slice. `/ct-groom` neither writes here nor rewrites what you write.)_\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- (fill in from the spec)\n\n## Gates\nBefore merging, these gates are closed by a HUMAN — the agent that implements the slice cannot consider them met:\n- **`plan`** — before implementing, a human has to review the slice's PLAN: the agent publishes it as a comment on this issue and stops. To give it the go, answer with a comment that is exactly `-OK <nonce>`, with the nonce /ct-next printed when it dispatched this slice (and nothing else: anything else starts nothing, on purpose). That nonce is not written in this issue because the agent reads the issue: it is the part of the permission it cannot manufacture, and without it `--release` refuses. If it has been lost, whoever dispatched reissues it with `scripts/ct-go.mjs`. What watches the issue is a process the coordinator launched on dispatching and that waits a few hours: if you answer much later it may have expired, and then the session has to be pushed by hand. The agent cannot give it as met.\n\n## Out of scope / Protected\n- (none declared)\n","state":"OPEN","stateReason":null,"milestone":null,"labels":{"nodes":[{"name":"gate:plan"},{"name":"status:in-review"}]}},{"number":56,"url":"https://github.com/jjponz/repo-pulse/issues/56","title":"CT-138 smoke B: document local-configuration","body":"> Plan asked for by hand: there is no ticket behind it.\n> To ask for changes to the plan, comment on this issue starting with `-REVIEW`: whatever you write after it is what gets asked of the agent, and it will publish the redone plan right here.\n\n## Descripción\nCT-138 smoke B: document local-configuration\n\n## Comentario de quien pide el plan\nCT-138 smoke B: document local-configuration\nThis is a disposable end-to-end verification of Control Tower, using this repository with its owner permission. Create only docs/verification/ct-138-local-configuration.md, in Spanish as required for this repository documentation. Document the REPO_PULSE_ROOT and REPO_PULSE_DATA_DIR environment variables, their defaults, and the read-only treatment of scanned repositories as declared in AGENTS.md. Keep the plan to one small documentation task. Do not change application code, configuration, existing tests, README.md or AGENTS.md. Follow the normal plan-review and implementation flow; stop after publishing the plan until the normal authorization arrives. Never merge the resulting pull request.\n\n## Contexto del epic\n_This plan does not come from any ticket._\n\n## Contexto heredado\n_(empty — the coordinating session fills it in when something already merged conditions this slice. `/ct-groom` neither writes here nor rewrites what you write.)_\n\n## Acceptance criteria (EARS, 1:1 con tests)\n- (fill in from the spec)\n\n## Gates\nBefore merging, these gates are closed by a HUMAN — the agent that implements the slice cannot consider them met:\n- **`plan`** — before implementing, a human has to review the slice's PLAN: the agent publishes it as a comment on this issue and stops. To give it the go, answer with a comment that is exactly `-OK <nonce>`, with the nonce /ct-next printed when it dispatched this slice (and nothing else: anything else starts nothing, on purpose). That nonce is not written in this issue because the agent reads the issue: it is the part of the permission it cannot manufacture, and without it `--release` refuses. If it has been lost, whoever dispatched reissues it with `scripts/ct-go.mjs`. What watches the issue is a process the coordinator launched on dispatching and that waits a few hours: if you answer much later it may have expired, and then the session has to be pushed by hand. The agent cannot give it as met.\n\n## Out of scope / Protected\n- (none declared)\n","state":"OPEN","stateReason":null,"milestone":null,"labels":{"nodes":[{"name":"gate:plan"},{"name":"status:in-progress"}]}}],"pageInfo":{"hasNextPage":false,"endCursor":"Y3Vyc29yOnYyOpK0MjAyNi0wOS0xMFQxMDowNjo1MlrPAAAAAUKCSM0="}}}}}]
--- stderr
