command: gh api graphql --paginate --slurp -f query=query($owner:String!,$name:String!,$headRefName:String!,$endCursor:String){repository(owner:$owner,name:$name){pullRequests(first:100,after:$endCursor,headRefName:$headRefName){nodes{number url body state isDraft headRefName headRefOid headRepository{nameWithOwner} baseRefName repository{nameWithOwner}}pageInfo{hasNextPage endCursor}}}} -f owner=mercadona -f name=control-tower -f headRefName=feat/550
version: gh version 2.100.0 (2026-09-03)
date: 2026-09-25
exit: 0
--- stdout
[{"data":{"repository":{"pullRequests":{"nodes":[{"number":605,"url":"https://github.com/mercadona/control-tower/pull/605","body":"Closes #550\n\n## What\n\nThe run driver family of the backend suite runs in process. Slice #2 of the milestone *The backend suite launches no process*.\n\n- `run-dispatch-real-process.test.ts`, `run-recovery-real-process.test.ts` and `run-driver-runtime-real-process.test.ts` are deleted and off `ProcessRatchet.LISTED`. Their cases live in `run-dispatch.test.ts`, `run-recovery.test.ts` and `run-driver-runtime.test.ts`, which launch nothing.\n- The four cases PR #563 owed to this slice have in-process substitutes. Each builds its admitted plan directly in the state, never through a `POST`: `run-driver.test.ts > a new admission reaches machine delivery with one conversation` replaces both admission cases; the later-fix and the kept-ownership cases live in `run-recovery.test.ts` and `run-driver-runtime.test.ts`.\n- The ledger `docs/superpowers/ledgers/2026-09-24-backend-without-processes.md` has 16 new rows, one per case this family lost.\n\nNew test infrastructure, all under `backend/__tests__/infrastructure/fixtures/`:\n\n- `ScriptedOracle` answers the `node` port of `CtRunMachine` with `StepAnnouncement` text, the plugin's own renderer (D-9). It throws `UnscriptedRequest` for anything nobody scripted.\n- `ScriptedClaude` plays a real `claude -p` capture; `InProcessWorkers` runs the real `HeadlessCallWorker` in this process with `spawn` and `kill` doubled.\n- `InProcessRun` composes the real `DriveRun` / `RunPlanAgents` / `RunPlanRecovery` graph; `git` answers from real captures through `ScriptedConversation` (D-8).\n- `ScriptedProcess` is now exported from `scripted-conversation.ts`.\n\nNothing under `backend/src/`, `plugin/` or `frontend/` changes. No endpoint changes, so `backend/API.md` reports no change; no data is stored, so there is no migration.\n\n## Decisions and deviations worth a look\n\n- **The review-watcher case doubles ports instead of `gh`.** No real `gh` capture can feed a review request: the last 60 pull requests carry only `APPROVED` reviews, and capturing an `issue edit` would change GitHub. The case doubles the `asked` port of `ReviewWatch` and the `Workbench` / `PlanIssues` ports; it asserts one `reopen` instead of the three `gh` argv, and keeps the fix call's descriptor, request id, prompt, measurement and model input. Plan §9 item 4.\n- **Coverage is not measured**, by the milestone owner's decision of 2026-09-24 (the issue's inherited context).\n- **Plan amendments during the run** (§9 items 9 and 10): the ledger row of the removed dispatch fixture case moved from Task 2 to Task 6, and Task 2's row count became `-ge 7`.\n\n## Findings for a person\n\n1. **A `ct-step controls` false positive.** It refuses a task that declares a removed test while any staged file names it — and a ledger row of that test must name it. Worked around inside the plan (the row landed one commit later), not in the control. Any later slice of this milestone that deletes a test and records it in the same task will hit it.\n2. **`HeadlessCallWorker.run()` closes its stdout/stderr handles right after the synchronous `spawn()`**, without waiting for the child. Correct for a real child (the OS duplicates the descriptor), `EBADF` for an in-process double. `ScriptedClaude` writes its capture synchronously; `src` is untouched.\n3. **The first `ct-step global` run was red with 6 load false reds** in `pty-live-sessions-real-process.test.ts` (5) and `ct-api-real-process.test.ts` (1), files of slices #7 and #5. Both passed 3/3 in isolation, and the second global run was green: 160 files, 3357 tests.\n\n## Verification\n\n- `npm run typecheck`: exit 0.\n- `env -u CT_STATE_DIR npx vitest run` from `backend/`: 160 files, 3357 tests passed (second run, see finding 3).\n- The five new in-process files passed 10/10 consecutive runs.\n- Task judges and slice judge: PASS; one low finding on record (`InProcessRun.create` takes an optional delivery the plan did not name).\n\nGenerated with Claude Code\n\nhttps://claude.ai/code/session_01JMr1ZJTtoXSMPo6Dm3DWgr\n","state":"MERGED","isDraft":false,"headRefName":"feat/550","headRefOid":"167cf9a4b7d9d46688cb817c734b6e581b1c8b44","headRepository":{"nameWithOwner":"mercadona/control-tower"},"baseRefName":"main","repository":{"nameWithOwner":"mercadona/control-tower"}}],"pageInfo":{"hasNextPage":false,"endCursor":"Y3Vyc29yOnYyOpHPAAAAARRyuJw="}}}}}]
--- stderr
